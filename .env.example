# Environment variables (copy to .env.local for local development)
# Note: The Free Dictionary API requires no authentication and has unlimited requests
# No API keys needed for this project!

# Optional: Add these if you implement backend features
# NEXT_PUBLIC_API_URL=http://localhost:3000
# ANALYTICS_KEY=your_analytics_key_here
